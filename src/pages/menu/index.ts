import { MenuBasket } from './menu-basket';
import { MenuContent } from './menu-content';
import { PageMenu } from './menu';

export const Menu = {
  Menu: PageMenu,
  Basket: MenuBasket,
  Content: MenuContent,
};
